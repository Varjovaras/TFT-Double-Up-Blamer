export type AccountV1Response = {
    puuid: string;
    gameName: string;
    tagLine: string;
};

export type TFTMatch = {
    matchId: string;
};

export type Player = {
    puuid: string;
    name: string;
    tagLine: string;
};
